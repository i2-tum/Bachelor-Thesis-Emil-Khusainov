Install in a new virtual environment Python 3.12 in this folder and change VENV_NAME in EnolaRunner.py for your new environment

To install dependencies into this venv:
pip install -r requirements.txt

Also here one could see an architectures folder, where a used from main.py architecture listed.
To work always need a pair jsons of one and with it "_fidelity" version
